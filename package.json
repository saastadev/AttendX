{
  "name": "attendx-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd attendx-v2 && npm install --include=dev && npm run build && mkdir -p .next/standalone/.next && cp -R .next/static .next/standalone/.next/ 2>/dev/null || true && cp -R public .next/standalone/ 2>/dev/null || true",
    "start": "cd attendx-v2 && node .next/standalone/server.js",
    "test": "cd attendx-v2 && npm run test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
